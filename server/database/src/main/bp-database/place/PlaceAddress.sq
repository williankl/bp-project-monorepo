CREATE TABLE IF NOT EXISTS PlaceAddress (
  id uuid PRIMARY KEY NOT NULL,
  street text NOT NULL,
  city text NOT NULL,
  country text NOT NULL,
  latitude double NOT NULL,
  longitude double NOT NULL
);