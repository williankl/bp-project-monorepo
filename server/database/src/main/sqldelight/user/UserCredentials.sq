createTableIfNeeded:
CREATE TABLE IF NOT EXISTS UserCredentials (
  userId uuid PRIMARY KEY NOT NULL,
  encryptedPassword text NOT NULL
);

retrieveCredentialsFor:
SELECT * FROM UserCredentials
WHERE userId = ?;